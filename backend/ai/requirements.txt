# Core AI/ML Libraries
# NOTE: torch/transformers/sentence-transformers are optional for local dev.
# In production with NVIDIA NIM, they are NOT needed (saves ~3GB Docker image size).
# Uncomment if you need local model inference:
# transformers>=4.35.0
# torch>=2.1.0
# sentence-transformers>=2.2.2
# layoutparser>=0.3.4

# NVIDIA NIM (OpenAI-compatible API)
openai>=1.40.0

# Hugging Face fallback (lightweight — no model downloads)
huggingface-hub>=0.20.0

Pillow>=10.1.0
scikit-learn>=1.3.0
numpy>=1.24.3

# Web Framework
fastapi>=0.104.1
uvicorn>=0.24.0
pydantic>=2.5.0
python-multipart>=0.0.6

# Document Parsing (kept — these run locally)
pdfplumber>=0.10.3
python-docx>=1.1.0
beautifulsoup4>=4.12.2
pytesseract>=0.3.10

# Web Scraping
crawl4ai>=0.4.3

# Database (pgvector for embeddings)
psycopg[binary]>=3.2
pgvector>=0.4.0
psycopg_pool>=0.2.0

# HTTP Client (for API calls)
httpx>=0.25.0

# Retry Logic
tenacity>=8.2.0

# Redis (response caching)
redis>=5.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.1

# Utilities
pyyaml>=6.0.0
